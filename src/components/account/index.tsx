import { View } from "react-native";
import AuthContainer from "../ui/AuthContainer";

const RenderAccount = () => {
  return (
    <AuthContainer>
        {/*children */}
            <View>
             
            </View>
    </AuthContainer>
  );
};
export default RenderAccount;
